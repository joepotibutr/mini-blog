import React from 'react'
import { CardWrapper } from './styled'

interface Props {
    
}

export default (props: Props) => {
    return (
        <CardWrapper>
            A Card
        </CardWrapper>
    )
}
